{
  "name": "ai-scheduler",
  "version": "1.0.0",
  "description": "A production-ready AI scheduling agent that creates Google Calendar events with Meet links from natural language commands.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.57.0"
  }
}
